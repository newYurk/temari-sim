name: Sim tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  sim-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 24]
    name: sim-tests (node ${{ matrix.node }})
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - name: Run sim test suite
        # Paths resolve via import.meta.url; CWD may be repo root.
        run: node sim/test/run.mjs
      - name: JS type-check (pinned tsc, no build, no deps in repo)
        # checkJs is off globally; files opt in with `// @ts-check` (see tsconfig.json).
        run: npx --yes -p typescript@5.9.3 tsc -p tsconfig.json
